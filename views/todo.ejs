<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Todo List</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 20px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        }

        h1 {
            color: #333;
            margin-bottom: 30px;
            font-size: 28px;
            text-align: center;
        }

        .add-todo-form {
            display: flex;
            gap: 10px;
            margin-bottom: 30px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 8px;
        }

        .add-todo-form input[type="text"] {
            flex: 1;
            padding: 12px 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s;
        }

        .add-todo-form input[type="text"]:focus {
            outline: none;
            border-color: #667eea;
        }

        .add-todo-form input[type="submit"] {
            padding: 12px 30px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .add-todo-form input[type="submit"]:hover {
            background: #5568d3;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        .todo-list {
            list-style: none;
        }

        .todo-list li {
            display: flex;
            align-items: center;
            padding: 15px;
            margin-bottom: 10px;
            background: #f8f9fa;
            border-radius: 8px;
            transition: all 0.3s;
            gap: 12px;
        }

        .todo-list li:hover {
            background: #e9ecef;
            transform: translateX(5px);
        }

        .todo-text {
            flex: 1;
            color: #333;
            font-size: 16px;
            word-break: break-word;
        }

        .action-btn {
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 6px;
            text-decoration: none;
            font-size: 18px;
            transition: all 0.3s;
            cursor: pointer;
        }

        .edit-btn {
            background: #ffc107;
            color: white;
        }

        .edit-btn:hover {
            background: #e0a800;
            transform: scale(1.1);
        }

        .delete-btn {
            background: #dc3545;
            color: white;
        }

        .delete-btn:hover {
            background: #c82333;
            transform: scale(1.1);
        }

        .empty-state {
            text-align: center;
            padding: 40px;
            color: #999;
            font-size: 18px;
        }

        .empty-state::before {
            content: "📝";
            display: block;
            font-size: 48px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📋 My Todo List</h1>
        
        <form action="/todo/add/" method="post" class="add-todo-form">
            <input 
                type="text" 
                name="newtodo" 
                id="newtodo" 
                autofocus 
                required
                placeholder="What should I do?" 
            />
            <input type="submit" value="Add Todo" />
        </form>

        <% if (todolist.length === 0) { %>
            <div class="empty-state">
                No todos yet. Add one above!
            </div>
        <% } else { %>
            <ul class="todo-list">
                <% todolist.forEach((todo, index) => { %>
                <li>
                    <a href="/todo/<%= index %>" class="action-btn edit-btn" title="Edit todo">✎</a>
                    <span class="todo-text"><%- todo %></span>
                    <a href="/todo/delete/<%= index %>" class="action-btn delete-btn" title="Delete todo">✘</a>
                </li>
                <% }); %>
            </ul>
        <% } %>
    </div>
</body>
</html>
